import React from 'react';
import { Helmet } from 'react-helmet-async';

const Login = () => {
    return (
        <div>
             <Helmet>
        <title>Login</title>
        </Helmet>
        </div>
    );
};

export default Login;