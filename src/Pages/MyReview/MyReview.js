import React from 'react';
import useTitle from '../../hooks/useTitle';

const MyReview = () => {
    useTitle('Mt Review')
    return (
        <div>
           review 
        </div>
    );
};

export default MyReview;